# Raw HTTP Copier 使用教程

## 目录

1. [简介](#简介)
2. [安装方法](#安装方法)
3. [界面说明](#界面说明)
4. [基本使用](#基本使用)
5. [功能详解](#功能详解)
6. [常见问题](#常见问题)

---

## 简介

Raw HTTP Copier 是一款 Chrome 开发者工具扩展，用于捕获并展示浏览器发出的 HTTP 请求和响应的原生报文内容。它支持：

- 实时捕获 Network 面板中的请求
- 左侧显示行号，方便定位报文内容
- 一键复制原生报文到剪贴板
- 一键下载原生报文为文本文件
- 自动适配系统亮/暗主题

---

## 安装方法

### 方式一：加载已解压的扩展程序（开发者模式）

1. 打开 Chrome 浏览器，在地址栏输入 `chrome://extensions/` 并回车
2. 在右上角打开 **开发者模式** 开关
3. 点击左上角的 **加载已解压的扩展程序** 按钮
4. 选择本项目的根目录（即包含 `manifest.json` 的文件夹，注意不是 `src` 目录）
5. 安装成功后，扩展图标会出现在扩展列表中

### 方式二：打包为 .crx（可选）

1. 在 `chrome://extensions/` 页面点击 **打包扩展程序**
2. 选择项目根目录，点击 **打包扩展程序**
3. 生成的 `.crx` 文件可拖拽安装到 Chrome 中

---

## 界面说明

安装成功后，打开任意网页，按 `F12` 或 `Ctrl+Shift+I` 打开 Chrome 开发者工具，在顶部标签栏中找到 **Raw HTTP** 标签。

面板整体分为三个区域：

| 区域 | 位置 | 说明 |
|------|------|------|
| 工具栏 | 顶部 | 包含 Clear All 按钮和请求计数 |
| 请求列表 | 左侧 | 显示已捕获的所有 HTTP 请求 |
| 报文详情 | 右侧 | 上半部分为请求报文，下半部分为响应报文 |

### 请求列表项

每个请求项显示以下信息：
- **Method**：HTTP 方法（GET、POST 等），加粗显示
- **Status**：HTTP 状态码，颜色区分（绿色=成功，黄色=重定向，红色=错误）
- **URL**：请求路径，过长时自动截断

点击任意请求项，右侧即可展示该请求的原生报文。

---

## 基本使用

### 1. 捕获请求

打开 **Raw HTTP** 面板后，刷新当前网页或触发页面上的网络请求，左侧列表会自动捕获并显示请求。最多保留最近 500 条请求。

### 2. 查看报文

在左侧列表中点击任意请求，右侧上半部分显示 **Raw Request**（原始请求报文），下半部分显示 **Raw Response**（原始响应报文）。

报文左侧带有行号，方便阅读和定位。

### 3. 复制报文

点击 **Raw Request** 或 **Raw Response** 区域右上角的 **Copy** 按钮，即可将该区域的完整原生报文复制到剪贴板。

### 4. 下载报文

点击 **Raw Request** 或 **Raw Response** 区域右上角的 **Download** 按钮，即可将报文下载为 `.txt` 文件。

- 请求文件命名格式：`request_{METHOD}.txt`，例如 `request_GET.txt`
- 响应文件命名格式：`response_{STATUS}.txt`，例如 `response_200.txt`

### 5. 清空列表

点击顶部工具栏的 **Clear All** 按钮，可清空所有已捕获的请求。

---

## 功能详解

### 原生报文格式

#### 请求报文示例

```http
GET /api/user?id=123 HTTP/1.1\r\n
Host: example.com\r\n
User-Agent: Mozilla/5.0 ...\r\n
Accept: application/json\r\n
\r\n
```

#### 响应报文示例

```http
HTTP/1.1 200 OK\r\n
Content-Type: application/json\r\n
Content-Length: 256\r\n
\r\n
{"id":123,"name":"Alice"}
```

> 注意：报文中 `\r\n` 为换行符，是 HTTP 协议规定的行分隔符。

### 主题适配

插件界面会自动跟随操作系统的亮/暗主题设置：
- 亮色主题：白色背景，深色文字
- 暗色主题：深色背景，浅色文字

无需手动切换。

---

## 常见问题

### Q1：面板中没有显示任何请求？

请确保：
1. 已打开 **Raw HTTP** 面板
2. 页面已刷新或已触发网络请求
3. 当前页面确实有网络活动（可在 Network 面板中确认）

### Q2：响应报文显示 "Loading response body..."？

响应体需要异步加载，通常在选中请求后 1~2 秒内会显示完整内容。如果长时间未加载，可能是该请求没有响应体（如 204 No Content）。

### Q3：下载的文件名可以自定义吗？

当前版本文件名自动生成（包含方法或状态码）。如需自定义文件名，可下载后手动重命名，或通过复制后粘贴到自定义文件中。

### Q4：为什么有些请求没有捕获到？

本插件通过 Chrome DevTools Network API 捕获请求，以下情况可能无法捕获：
- 页面加载完成前已发出的请求（需在页面加载前打开面板）
- 某些内部 Chrome 请求（如扩展自身的请求）
- Service Worker 拦截的请求（取决于 Chrome 版本行为）

### Q5：如何更新扩展？

1. 修改源码后保存
2. 进入 `chrome://extensions/`
3. 找到 **Raw HTTP Copier**，点击 **刷新** 按钮（圆形箭头图标）

---

## 技术信息

- **Manifest Version**：3
- **权限**：无需额外权限
- **最大保留请求数**：500 条
- **支持浏览器**：Chrome 88+（支持 Manifest V3 的版本）
